import styled from "@emotion/styled";

const AppComponent = styled.div({
    textAlign: "center",
    backgroundColor: "#bdf5f2",
    width: "100%",
})

function App() {
  return (
    <AppComponent>
      Something
    </AppComponent>
  );
}

export default App;
